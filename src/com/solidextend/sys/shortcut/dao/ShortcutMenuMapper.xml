<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.solidextend.sys.shortcut.dao.ShortcutMenuMapper">
	<resultMap id="BaseResultMap" type="com.solidextend.sys.shortcut.vo.ShortcutMenu">
		<id column="MODULE_ID" property="moduleId" jdbcType="VARCHAR" />
		<id column="USER_ID" property="userId" jdbcType="VARCHAR" />
	</resultMap>

	<delete id="delete">
		delete from SYS_SHORTCUT_MENU where MODULE_ID = #{moduleId,jdbcType=VARCHAR}
		and USER_ID = #{userId,jdbcType=VARCHAR}
	</delete>

	<delete id="deleteByUserId" parameterType="string">
		delete from SYS_SHORTCUT_MENU where MODULE_ID = USER_ID =
		#{userId,jdbcType=VARCHAR}
	</delete>

	<insert id="insert" parameterType="com.solidextend.sys.shortcut.vo.ShortcutMenu">
		insert into SYS_SHORTCUT_MENU
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="moduleId != null">
				MODULE_ID,
			</if>
			<if test="userId != null">
				USER_ID,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="moduleId != null">
				#{moduleId,jdbcType=VARCHAR},
			</if>
			<if test="userId != null">
				#{userId,jdbcType=VARCHAR},
			</if>
		</trim>
	</insert>

</mapper>